//This might become code to allow for adjustments of the style via Foundry
//Maybe also it will become a way to manage the notes in Foundry

Hooks.once('init', () => {
  console.log('Obsidian2Foundry ready!');
  // Register custom callouts, Markdown importers, etc.
});
